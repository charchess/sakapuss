appId: com.sakapuss.mobile
---
- launchApp:
    clearState: true
# Dismiss Expo dev menu if it appears on launch (menu can appear 2-3s after launch)
- waitForAnimationToEnd
- waitForAnimationToEnd
- runFlow:
    file: ../helpers/dismiss-dev-menu.yaml
    when:
      visible: "Continue"
- waitForAnimationToEnd
- assertVisible: "Se connecter"
- tapOn:
    id: "email-input"
- inputText: "playwright-e2e@test.sakapuss.local"
- tapOn:
    id: "password-input"
- inputText: "PlaywrightTest123!"
- tapOn: "Se connecter"
- assertVisible: "Que se passe-t-il aujourd'hui ?"
- assertNotVisible: "Erreur"

# Naviguer vers la fiche de l'animal
- assertVisible:
    text: "Fiche de"
    optional: true
- tapOn:
    text: "Fiche de"
    optional: true

# Vérifier le contenu de la fiche — pas d'erreur
- assertNotVisible: "Impossible de charger"
- assertNotVisible: "Erreur"
- assertVisible:
    text: "Événements récents"
    optional: true
